# قائمة فحص النشر النهائية — مستقبلي كاب

مراجعة QA شاملة قبل النشر على الإنتاج. تغطي الأمان، سياسات RLS، الاستجابة على الجوال،
صلاحيات الإدارة، التكلفة والأداء على Supabase، سيناريو 1000+ طلب، والتأكد من عدم وصول
الزوار لبيانات الإدارة. الإصلاحات الموصوفة هنا **مطبّقة بالفعل** في الكود الحالي —
هذا المستند يوثّق ما تم فحصه وما يجب عليك التحقق منه يدوياً في مشروع Supabase الفعلي
قبل الإطلاق.

> **ملاحظة تحديث:** القسم 3 أدناه (الاستجابة على الجوال) يعود لإصدار كان فيه `index.html`
> موقعاً تسويقياً تقليدياً بشريط تنقّل. تم استبدال ذلك لاحقاً بتطبيق شاشة واحدة (PWA) —
> راجع `README.md` لتفاصيل التصميم الجديد. القسمان 1، 2، 4-7 (الأمان، RLS، صلاحيات الإدارة،
> التكلفة، سيناريو 1000+ طلب، عزل بيانات الإدارة) ما زالا ساريَين بالكامل دون تغيير، لأن
> إعادة التصميم لم تمسّ الخادم أو RLS أو لوحة الإدارة.

---

## 1) مراجعة الأمان (Security Review)

| البند | الحالة | التفاصيل |
|---|---|---|
| مفتاح `service_role` في كود العميل | ✅ لا يوجد | يظهر فقط داخل Edge Function التي تعمل على خادم Supabase، وليس في أي ملف JS يُحمَّل بالمتصفح |
| حقن HTML/XSS في لوحة الإدارة | ✅ مُعالَج | كل قيم العملاء (`customer_name`, `phone`, `pickup_location`, `driver_name`...) تمر عبر `escapeHtml()` قبل إدراجها في DOM. قيم `service_type`/`status` (مقيّدة أصلاً بـ CHECK في قاعدة البيانات) تمت إضافة تنقية دفاعية إضافية (`escapeAttr`) لها كخط دفاع ثانٍ |
| رؤوس أمان HTTP (CSP, X-Frame-Options...) | ✅ مضافة | ملف `netlify.toml` يضبط Content-Security-Policy، X-Frame-Options، Referrer-Policy، Permissions-Policy (مع سماح صريح لـ `geolocation` حتى لا يكسر زر "موقعي الحالي") |
| نقطة نهاية Edge Function مفتوحة للجميع | ✅ مُعالَج | كانت الدالة `notify-whatsapp` قابلة للاستدعاء من أي شخص يملك مفتاح anon العام (وهو مفتاح علني بالتصميم)، ما يسمح بإرسال رسائل واتساب مزوّرة باسم الشركة. أُضيف تحقق من ترويسة `x-webhook-secret` مقابل سر `WEBHOOK_SECRET` |
| مدخلات المستخدم غير محدودة الطول | ✅ مُعالَج | أُضيفت قيود CHECK على طول الحقول النصية في `schema.sql` (`customer_name` ≤100، `notes` ≤500، `phone` ≤20، `pickup_location`/`dropoff_location` ≤300، `driver_name` ≤100، `driver_phone` ≤20) لمنع إغراق الجدول ببيانات ضخمة. الحد نفسه مطبّق الآن أيضاً على مستوى الواجهة (`maxlength` على حقول `index.html` و`admin.html`) كخط دفاع أول قبل وصول الطلب لقاعدة البيانات |
| حماية CAPTCHA/rate-limit لتسجيل دخول الإدارة | ⚠️ يتطلب إعداد يدوي | Supabase Auth يوفر حداً افتراضياً لمحاولات الدخول، لكن يُنصح بتفعيل hCaptcha من Authentication → Settings → Bot and Abuse Protection قبل الإطلاق |
| تعطيل التسجيل العام (public sign-up) | ⚠️ يتطلب إعداد يدوي | راجع القسم 4 أدناه |

---

## 2) مراجعة سياسات RLS (Row Level Security)

**اكتشاف رئيسي تم إصلاحه:** الإصدار السابق من `schema.sql` كان يمنح `anon` صلاحية INSERT مباشرة
على `trip_requests` عبر سياسة RLS، بينما `request.js` كان يستدعي `.insert().select().single()`.
بما أن `anon` لم يكن يملك أي سياسة SELECT، فإن جملة `RETURNING` (التي تخضع لسياسات SELECT في
Postgres) كانت تُعيد صفاً فارغاً — أي أن **كل عملية إرسال طلب كانت ستفشل بصمت** وتُظهر رسالة خطأ
للعميل رغم أن الطلب قد يكون أُدرج فعلياً في قاعدة البيانات. كما أن منح `anon` صلاحية INSERT
مباشرة على الجدول بالكامل كان يسمح نظرياً بإرسال قيم تعسفية لأي عمود (مثل `status: 'completed'`
أو `driver_name`) عبر استدعاء REST مباشر متجاوزاً نموذج الموقع.

**الإصلاح:** تمت إزالة سياسة INSERT العامة تماماً. الإرسال العام يمر الآن حصراً عبر دالة
`submit_trip_request()` بصلاحية `SECURITY DEFINER`، والتي:
- تقبل فقط الحقول المحددة (لا يمكن للزائر تعيين `status` أو `driver_name` أو أي عمود آخر).
- تضبط `status = 'new'` دائماً.
- تُعيد فقط `id` و`request_number` (وليس بيانات العميل الكاملة).

| الجدول | INSERT | SELECT | UPDATE |
|---|---|---|---|
| `trip_requests` | لا سياسة لـ anon (عبر RPC فقط) | `is_admin()` فقط | `is_admin()` فقط |
| `trip_status_history` | (عبر trigger فقط، بصلاحيات المالك) | `is_admin()` فقط | لا سياسة (سجل تاريخي، لا تعديل) |
| `whatsapp_notifications` | (عبر trigger فقط) | `is_admin()` فقط | لا سياسة (الـ Edge Function تستخدم service_role الذي يتجاوز RLS) |
| `admins` | لا سياسة لأحد | المستخدم يرى صفّه فقط (`user_id = auth.uid()`) | لا سياسة |

**تحقق يدوي قبل الإطلاق:**
- [ ] نفّذ `schema.sql` كاملاً على مشروع Supabase (آمن لإعادة التنفيذ).
- [ ] من SQL Editor: `select * from trip_requests limit 1;` بحساب anon (أو عبر REST بمفتاح anon فقط) — يجب أن يُعيد **صفراً صفوف** أو خطأ صلاحيات، وليس بيانات.
- [ ] جرّب إرسال طلب فعلي من `index.html` (شاشة "تفاصيل الرحلة") وتأكد من ظهور رقم الطلب في شاشة "حالة الطلب" (يثبت أن RPC تعمل بشكل صحيح).

---

## 3) مراجعة الاستجابة على الجوال (Mobile Responsiveness)

> ⚠️ **تحديث معماري:** واجهة العميل استُبدلت بتطبيق شاشة واحدة (`index.html` الجديد: خريطة +
> قائمة سفلية) — انظر "تجربة العميل" في README. الصفحة التسويقية القديمة متعددة الصفحات
> (بقائمة hamburger وقسم `.hero-stats`) لم تعد موجودة، والصفوف الخاصة بها أدناه أصبحت تاريخية.
> صفوف لوحة الإدارة (`admin.html`) ما زالت سارية بلا تغيير.

| البند | الحالة | التفاصيل |
|---|---|---|
| ~~قائمة تنقّل الجوال (hamburger)~~ | ℹ️ لم يعد ينطبق | كانت لصفحة تسويقية متعددة الصفحات، استُبدلت بتطبيق شاشة واحدة بلا تنقّل صفحات أصلاً |
| ~~فيض إحصائيات الصفحة الرئيسية (`.hero-stats`)~~ | ℹ️ لم يعد ينطبق | القسم نفسه غير موجود في التطبيق الجديد |
| تكبير Safari التلقائي عند تركيز الحقول | ✅ مُعالَج | 16px على حقول الإدخال في كل من تطبيق العميل الجديد ولوحة الإدارة |
| فيض أفقي في شريط أدوات الإدارة | ✅ مُعالَج | لوحة الإدارة مبنية بنفس مبدأ scroll أفقي مضبوط داخل `.admin-table-wrap` |
| حجم لمس الأزرار | ✅ جيد | ≥48px في تطبيق العميل الجديد (بعضها 56-84px)، ≥44px في لوحة الإدارة |
| `dir="rtl"` و`lang="ar"` | ✅ صحيح على كل الصفحات | يشمل `admin.html` الجديدة |
| **جديد —** قائمة سفلية قابلة للسحب (bottom sheet) | ✅ | تعمل عبر Pointer Events مع snap لثلاث ارتفاعات؛ `touch-action` مضبوط بحيث لا يتعارض السحب مع تمرير محتوى القائمة نفسها |
| **جديد —** مناطق آمنة لشاشات iPhone ذات النوتش | ✅ | `env(safe-area-inset-*)` مطبّقة على الشريط العلوي، القائمة السفلية، والتوست |
| **جديد —** خريطة كاملة الشاشة على أحجام شاشة مختلفة | ⚠️ يتطلب تحقق يدوي | Leaflet يعيد قياس نفسه عبر `invalidateSize()` عند تغيّر حجم النافذة، لكن يستحق تجربة فعلية عند إظهار/إخفاء شريط عنوان Safari على iOS |

**تحقق يدوي قبل الإطلاق:**
- [ ] افتح الموقع فعلياً على جوال حقيقي (iOS Safari + Android Chrome)، ليس فقط أدوات المطور.
- [ ] اختبر iPhone SE (375×667) وأي جهاز بنوتش (Dynamic Island/notch) للتأكد من مناطق الأمان.
- [ ] جرّب سحب القائمة السفلية يدوياً بين الحالات الثلاث (مطوية/نصف/كاملة) بإصبعك الفعلي، وليس بالماوس فقط.
- [ ] تأكد من أن متصفح الجوال يطلب إذن الموقع الجغرافي بشكل صحيح عند فتح التطبيق أو الضغط على "موقعي" (يتطلب HTTPS — يعمل تلقائياً على Netlify).
- [ ] اختبر "إضافة إلى الشاشة الرئيسية" فعلياً على iOS وAndroid وتأكد من ظهور الأيقونة والاسم الصحيحين.

---

## 4) مراجعة صلاحيات الإدارة (Admin Authorization)

**اكتشاف رئيسي تم إصلاحه:** كانت سياسات RLS تستخدم `to authenticated using (true)` — أي أن
**أي شخص يسجّل حساباً جديداً** عبر Supabase Auth (التسجيل العام مفعّل افتراضياً في مشاريع
Supabase الجديدة، ويمكن لأي شخص يملك مفتاح anon العام إنشاء حساب عبر واجهة Auth مباشرة، دون
الحاجة لصفحة `admin.html` أصلاً) كان سيحصل تلقائياً على صلاحية قراءة وتعديل **كامل بيانات
العملاء** (الأسماء، أرقام الجوال، مواقع الانطلاق، إلخ) بمجرد تسجيل الدخول.

**الإصلاح:** أُضيف جدول `admins` كقائمة سماح صريحة، ودالة `is_admin()` التي تتحقق من عضوية
المستخدم فيه. جميع سياسات القراءة/التعديل الآن تستخدم `is_admin()` بدلاً من `authenticated`
المطلقة. لا توجد سياسة INSERT/UPDATE على جدول `admins` نفسه — الإضافة تتم يدوياً فقط عبر
SQL Editor (الذي يستخدم صلاحيات المالك ويتجاوز RLS).

**تحقق يدوي قبل الإطلاق (إلزامي):**
- [ ] أنشئ مستخدم الإدارة من Authentication → Users.
- [ ] انسخ الـ User UID الخاص به وأضفه لجدول `admins`:
      `insert into admins (user_id) values ('...');`
- [ ] **اختبر السيناريو السلبي**: سجّل حساباً ثانياً تجريبياً (بريد مختلف) ولا تضِفه لجدول
      `admins`، وتأكد من أن تسجيل الدخول به على `admin.html` لا يُظهر أي طلبات (يجب أن تكون
      اللوحة فارغة أو تُظهر رسالة تعذّر التحميل، وليس بيانات العملاء).
- [ ] من Authentication → Settings: عطّل **"Allow new users to sign up"** (أو قيّده بنطاق
      بريدك)، كطبقة حماية إضافية — حتى لو نُسي هذا، جدول `admins` يمنع الوصول لأي حال.

---

## 5) مراجعة التكلفة والأداء على Supabase (Cost & Performance)

| البند | الحالة | التفاصيل |
|---|---|---|
| فهارس (indexes) على الأعمدة المستخدمة في الفرز/التصفية | ✅ مضافة | `created_at`, `status`, `trip_id` (على جدولي التاريخ والإشعارات) |
| تحميل غير محدود لكل الصفوف في لوحة الإدارة | ✅ مُعالَج | `loadRequests()` في `js/admin.js` يجلب أحدث 300 طلب فقط عبر `.order('created_at', {ascending:false}).limit(300)` بدل الجدول بالكامل |
| استعلامات RLS المتكررة (`is_admin()`) | ✅ فعّالة | الدالة `stable` وتبحث بمفتاح أساسي مفهرس (`user_id`)، فالتكلفة زهيدة حتى مع مئات الطلبات |
| تنبيهات واتساب متكررة/فاشلة تتراكم | ⚠️ يتطلب متابعة | `whatsapp_notifications.sent` يبقى `false` عند فشل الإرسال، دون آلية إعادة محاولة تلقائية. راقب هذا الجدول دورياً أو أضف تنظيفاً/إعادة محاولة لاحقاً |
| حجم استعلام `SELECT *` | ℹ️ مقبول حالياً | عدد الأعمدة صغير (~15 عمود نصي/تاريخ)، لا داعٍ لتحديد أعمدة بعينها في هذا الحجم |

**تقديرات التكلفة التقريبية (خطة Supabase المجانية):**
- قاعدة بيانات: 500MB — تكفي لعشرات آلاف الطلبات بسهولة نظراً لصغر حجم الصف الواحد.
- Egress شهري: 5GB — مع حد `.limit(300)` في لوحة الإدارة، كل تحميل صفحة إدارة يجلب ≈ 300 صف
  فقط بدلاً من الجدول بالكامل، ما يبقي الاستهلاك منخفضاً حتى مع آلاف الطلبات المتراكمة تاريخياً.
- Edge Function invocations: 500K/شهر — إشعار واتساب واحد لكل طلب جديد فقط، بعيد جداً عن الحد.

**تحقق يدوي قبل الإطلاق:**
- [ ] راجع Supabase Dashboard → Database → Database Size بعد فترة تشغيل تجريبية.
- [ ] إن توقعت أكثر من بضع مئات طلب/يوم بشكل مستمر، خطط للترقية لخطة Pro قبل الوصول لحدود
      الخطة المجانية (خصوصاً Egress).

---

## 6) اختبار سيناريو 1000+ طلب

لا يمكن تنفيذ اختبار حمل فعلي على مشروع Supabase حي من هذه البيئة، لكن تمت مراجعة السلوك
المتوقع نظرياً وتوفير سكربت اختبار جاهز (أرقام الهاتف التجريبية بصيغة عراقية `07` — لا صيغة
سعودية `05` كما في نسخة سابقة من هذا المستند):

| الجانب | التقييم |
|---|---|
| تفرّد `request_number` تحت تزامن عالٍ | ✅ آمن — يعتمد على `nextval()` على sequence، وهي عملية ذرية (atomic) في Postgres، لا تصادم ممكن حتى مع إدراجات متزامنة |
| أداء لوحة الإدارة مع 1000+ صف | ✅ محسَّن — بفضل `.limit(300)` + فهرس `created_at`، وقت الاستعلام يبقى تحت المللي ثانية تقريباً بدل مسح الجدول كاملاً |
| البحث/التصفية في الواجهة | ℹ️ ملاحظة | البحث والتصفية حالياً تتم على الصفوف المحمّلة في المتصفح (أحدث 300 طلب) وليس عبر استعلام SQL. طلب قديم جداً (خارج أحدث 300) لن يظهر بالبحث. هذا سلوك مقصود لضبط التكلفة حالياً (لا حاجة لتغييره الآن، لكن يستحق إضافة بحث من جهة الخادم أو ترقيم صفحات لاحقاً إن كبر الحجم كثيراً) |
| تريغرات (`log_trip_status`, `queue_whatsapp_notification`) | ✅ آمنة تحت الحمل — عمليات بسيطة (INSERT واحد لكل تريغر)، لا حلقات أو استعلامات مكلفة |

**سكربت اختبار (شغّله في مشروع تجريبي/staging فقط، وليس الإنتاج):**
```sql
-- يولّد 1000 طلب وهمي عبر نفس مسار الإنتاج (submit_trip_request) للتحقق من
-- عدم تصادم request_number وسرعة تحميل لوحة الإدارة بعدها.
-- أرقام الهاتف بصيغة عراقية: 07 + 9 أرقام (11 رقماً إجمالاً).
do $$
begin
  for i in 1..1000 loop
    perform submit_trip_request(
      (array['taxi','private','courier','intercity'])[1 + (i % 4)],
      'عميل اختبار ' || i,
      '07' || lpad(i::text, 9, '0'),
      'موقع اختبار ' || i,
      null, null, null, null, null
    );
  end loop;
end $$;

-- تحقق من عدم وجود تكرار في أرقام الطلبات
select request_number, count(*) from trip_requests group by request_number having count(*) > 1;
-- يجب أن تُعيد صفراً صفوف
```

**تحقق يدوي قبل الإطلاق:**
- [ ] شغّل السكربت أعلاه على مشروع staging منفصل (وليس الإنتاج).
- [ ] افتح `admin.html` وتأكد من أن التحميل الأولي سريع (أقل من ثانيتين تقريباً).
- [ ] احذف بيانات الاختبار من مشروع staging قبل استخدامه لأي غرض آخر.

---

## 7) التأكد من عدم وصول الزوار لبيانات الإدارة

هذا البند مغطّى فعلياً بإصلاحات القسمين 2 و4 أعلاه، لكن كخلاصة مباشرة:

- [x] لا توجد سياسة SELECT لـ `anon` على `trip_requests` أو `trip_status_history` أو
      `whatsapp_notifications` — أي طلب REST مباشر بمفتاح anon فقط (بدون تسجيل دخول) يُعاد
      فارغاً أو مرفوضاً.
- [x] تسجيل حساب Auth جديد وحده **لا يكفي** — يجب إضافة `user_id` يدوياً لجدول `admins`.
- [x] دالة `submit_trip_request()` تُعيد فقط `id` و`request_number` — لا اسم عميل، لا جوال،
      لا موقع لأي طرف آخر غير المُرسِل نفسه (وحتى هو لا يستعلم بياناته لاحقاً، فقط يحصل على
      رقم مرجعي وقت الإرسال).
- [ ] **تحقق يدوي نهائي (افعله فعلياً قبل الإطلاق):** افتح أدوات المطور في متصفح، وبدون تسجيل
      دخول، نفّذ من الـ Console على `index.html`:
      ```js
      fetch('https://YOUR-PROJECT.supabase.co/rest/v1/trip_requests?select=*', {
        headers: { apikey: 'YOUR-ANON-KEY', Authorization: 'Bearer YOUR-ANON-KEY' }
      }).then(r => r.json()).then(console.log)
      ```
      النتيجة المتوقعة: مصفوفة فارغة `[]`. أي بيانات فعلية تظهر هنا = **مشكلة حرجة يجب حلها
      قبل النشر**.

---

## قائمة الفحص النهائية قبل الضغط على Deploy

- [ ] نُفّذ `schema.sql` الكامل على مشروع Supabase الفعلي.
- [ ] `js/config.js` يحتوي على `SUPABASE_URL` و`SUPABASE_ANON_KEY` الصحيحين (وليس القيم
      الافتراضية placeholder).
- [ ] حساب إدارة واحد على الأقل تم إنشاؤه وإضافته لجدول `admins`.
- [ ] تم اختبار إرسال طلب فعلي من `index.html` ورؤية رقم الطلب في شاشة "حالة الطلب".
- [ ] تم اختبار تسجيل الدخول للوحة الإدارة (`admin.html`) ورؤية الطلب المُرسل فيها.
- [ ] تم تنفيذ فحص "تحقق يدوي نهائي" في القسم 7 أعلاه والتأكد من نتيجة `[]`.
- [ ] (اختياري) تم نشر Edge Function واختبار وصول تنبيه واتساب فعلي.
- [ ] تعطيل التسجيل العام في Authentication → Settings.
- [ ] تفعيل حماية bot/CAPTCHA لتسجيل الدخول في Authentication → Settings.
- [ ] `netlify.toml` مرفوع مع باقي الملفات (يُطبَّق تلقائياً عند النشر على Netlify).
- [ ] تحديث `BUSINESS_WHATSAPP_NUMBER` في `js/app.js` برقم الشركة الفعلي (حالياً placeholder بمقدمة 964).
- [ ] نسخة احتياطية أولية (Supabase يوفر نسخاً احتياطية تلقائية على الخطط المدفوعة — تحقق من
      إعداداتك إن كانت البيانات حرجة).
