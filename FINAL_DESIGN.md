# مستقبلي كاب — التصميم النهائي: تعيين السائق (محدد/FIFO) + Push — للمراجعة فقط

**لا شيء نُفّذ على قاعدة بياناتك أو Supabase. لا مفتاح VAPID خاص بأي ملف هنا أو بالـZIP.**

## ⚠️ افتراض لازم أؤكده معك
بند (1) يقول "إذا كتب الزبون اسم سائق محدد". فحصت `index.html` الحالي — **لا يوجد فيه أي حقل نصي حر لكتابة اسم سائق**. الموجود فعليًا هو زر "طلب" على بطاقة سائق معيّن بقائمة السواق (مبني مسبقًا، مرتبط برقم الهاتف لا بالاسم). لذلك فسّرت "كتب اسم سائق محدد" على إنها **نفس آلية اختيار سائق معيّن من القائمة** (تعبير عامي لـ"حدد/سمّى سائق")، وليس حقل كتابة نص حر جديد. هذا التفسير أدق تقنيًا أيضًا لأن **المطابقة بالاسم غير موثوقة** (احتمال تكرار الاسم بين سائقين)، بعكس رقم الهاتف الفريد أصلًا بالنظام. **إذا كنت فعلاً تقصد حقل كتابة اسم حر جديد، هذا تصميم مختلف يحتاج `index.html` الحالي + نقاش حل تعارض الأسماء المكررة.**

## 🐛 خطأ حرج لقيته أثناء الفحص وصحّحته: ازدواج العدّاد (Double-counting)
تصميمي بالجولة السابقة كان يُبقي على استدعاء `select_driver()` من `app.js` بعد كل حجز ناجح، **بينما التصميم الجديد يتطلب تعيين FIFO التلقائي يصير داخل `submit_trip_request` نفسها**. لو تركت الاثنين مع بعض، كل حجز كان راح يُحرّك `last_served_at`/`request_count` **مرتين** لنفس السائق — يكسر عدالة الطابور فعليًا. **صحّحته**: حذفت استدعاء `select_driver()` من `app.js` نهائيًا؛ التحريك الآن يصير **مرة وحدة بالضبط**، داخل `submit_trip_request` ذاتها، لكلا المسارين (تحديد صريح أو FIFO تلقائي).

## التصميم النهائي

### `submit_trip_request` (نسخة أخيرة، بديلة عن مسودة الجولة الماضية)
- **زبون حدد سائق معيّن** → تحقق فوري إنه لسا نشط بنفس الخدمة → تعيين مباشر + تحريك طابوره.
- **زبون ما حدد أحد** → **جديد بهذا التصميم**: اختيار تلقائي لصاحب الدور (نفس ترتيب `get_front_driver()` تمامًا: `active=true`, `last_served_at asc`, `created_at asc`) + تحريك طابوره — **ذرّيًا** (`FOR UPDATE SKIP LOCKED`) لمنع تعارض حجزين متزامنين يمسكان نفس السائق.
- **لا يوجد سائق متاح بالحالتين** → الطلب يُنشأ `status='new'` بدون سائق، تمامًا كسلوك اليوم — الحجز نفسه لا يفشل أبدًا.

### الإشعارات — بدون أي تعديل إضافي مطلوب
تريغرات `migration_push_notifications.sql` (من الجولة السابقة) تعتمد أصلاً على **تغيّر `driver_phone`**، بغض النظر عن كيف تغيّر — فتغطي المسارين الجديدين (التحديد الصريح + FIFO التلقائي) **تلقائيًا وبدون أي تعديل على أي تريغر**. إشعار الإدارة (`AFTER INSERT` غير مشروط) يغطي كل الحالات أصلًا.

### GPS
لم يُلمس إطلاقًا — يبقى فقط لعرض/ترتيب قائمة السواق بصريًا (`sortRosterByZone`)، مصدر الحقيقة للتعيين الفعلي يبقى `last_served_at`/`created_at` حصرًا، تمامًا كما طلبت.

## ✅ تأكيد صفر تأثير
| النظام | الحالة |
|---|---|
| `get_front_driver()`, `select_driver()` | لم تُلمسا — لا زالتا موجودتين لأي استخدام مستقبلي، فقط `app.js` توقف عن استدعاء `select_driver()` بعد الحجز (لتفادي الازدواج) |
| WhatsApp (`whatsapp_notifications`) | لم يُلمس |
| GPS (`driver_locations`, `update_driver_location`) | لم يُلمس |
| Telegram | غير مستخدم بتاتًا |

## الملفات بهذا الـZIP
| الملف | الحالة |
|---|---|
| `migration_submit_trip_request_final.sql` | **جديد، نهائي** — يلغي مسودة `submit_trip_request` بالجولة الماضية |
| `migration_push_notifications.sql` | من الجولة الماضية، بدون تغيير — لا يزال صحيحًا كما هو |
| `app.js` | معدّل: حذف استدعاء `select_driver()` المكرر فقط |
| `admin.js`, `admin.html`, `driver.js`, `driver.html`, `driver.css`, `admin-sw.js`, `driver-sw.js` | من الجولة الماضية، بدون تغيير إضافي بهذه الجولة |
| `supabase_functions/send-push-notification.ts` | بدون تغيير — **تأكيد: يقرأ المفتاح الخاص من env فقط، صفر مفتاح حرفي بالملف** |

## 🔵 قرارك المسجَّل: معالجة السائق المشغول مؤجَّلة عمدًا
لم يتغيّر أي شرط باختيار السائق أو `get_front_driver()` بهذه المرحلة — الفجوة الموصوفة أعلاه (سائق "في مهمة" نظريًا قابل للحجز عليه ضمن فارق ثوانٍ) **موجودة أصلًا بالمشروع الأصلي ولم تُمس**، بقرارك الصريح، لضمان التوافق الكامل وعدم إدخال أي تغيير وظيفي جديد بهذه المرحلة. تُعالَج لاحقًا كمرحلة مستقلة بعد اكتمال النظام واختباره.

## 🔴 غير مُنفَّذ عمدًا (بانتظار قرارك)
لا SQL نُفّذ، لا Edge Function نُشرت، لا مفتاح VAPID خاص تم توليده أو وضعه بأي ملف بهذه الجولة — عليك توليده بنفسك عند التنفيذ الفعلي فقط.
